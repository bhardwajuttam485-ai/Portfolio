PORT=5000
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.ryejjyk.mongodb.net/portfolioDB?retryWrites=true&w=majority
EMAIL_USER=your-email@example.com
EMAIL_PASS=your-email-password
RECEIVER_EMAIL=your-email@example.com

# If you use MongoDB Atlas, make sure:
# 1. the username and password are correct
# 2. your cluster IP access list allows your machine
# 3. the database name matches the one in the URI
