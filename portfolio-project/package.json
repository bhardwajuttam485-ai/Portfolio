{
  "name": "portfolio-project",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix portfolio-backend\" \"npm run dev --prefix portfolio-ui\"",
    "start": "npm run dev",
    "backend": "npm run dev --prefix portfolio-backend",
    "frontend": "npm run dev --prefix portfolio-ui"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "react-router-dom": "^7.14.0"
  }
}
